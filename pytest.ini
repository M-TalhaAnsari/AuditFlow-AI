[pytest]
markers =
    integration: needs the built FAISS index and/or live Ollama/Groq/Gemini calls -- run with `pytest -m integration`, or `pytest -m "not integration"` to skip them (that's also the default, see conftest.py)
testpaths = tests